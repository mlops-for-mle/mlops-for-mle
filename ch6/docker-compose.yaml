version: "3"

services:
  api-with-model:
    platform: linux/amd64
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - 8000:8000
    container_name: api-with-model

networks:
  default:
    name: mlo-mle-network
